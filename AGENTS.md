# Project Conventions

## Changelog

- If the project contains a `CHANGELOG.md`, update it whenever you make a meaningful user-facing change, bug fix, dependency change, or build/release-related change.
- Add new notes under `Unreleased` unless the user explicitly asks for a versioned release entry.
